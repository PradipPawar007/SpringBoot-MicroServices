
spring:
  application:
    name: GREET-API
  
  boot:
    admin:
      client:
        url: http://localhost:1111/

  cloud:
    compatibility-verifier:
      enabled: false
      
management:
  endpoints:
    web:
      exposure:
        include: '*'
        